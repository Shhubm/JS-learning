const name = "shhubm "
const repoCount = "10"

// console.log("name"+"repoCount") there's a better way to do stings

console.log(`my name is ${name} and my repo count is${repoCount}`);
// iska kya scene hai k you have to use back ticks bole toh (  ``  ) ye or (${}) ye variable dalne k lea